#include <stdio.h>

/**
 * main - prints all digit numbers of base 10 using putchar
 *
 * Return:Always 0 (Success)
 */

int main(void)
{
	int x;

	for (x = 48; x< 58; c++)
	{
		putchar(x);
	}
	putchar('\n');
	return (0);
}
